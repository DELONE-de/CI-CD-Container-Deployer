version: 0.2

phases:
  pre_build:
    commands:
      - echo Logging in to Amazon ECR...
  build:
    commands:
      - echo Building Docker image...
  post_build:
    commands:
      - echo Pushing Docker image...
